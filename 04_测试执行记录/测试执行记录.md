# 测试执行记录

- 文档编号：EXEC-RYVUE-001
- 参考依据：《测试用例.xlsx》（61 条）
- 执行周期：2026.08.01 - 2026.08.08
- 执行人员：谭锦坤
- 测试环境：RuoYi-Vue 本地部署（localhost:81）/ Chrome / Edge / Postman / MySQL

---

## 一、执行概况

| 统计项 | 数量 |
|---|---|
| 总用例数 | 61 |
| 已执行 | 61 |
| 通过（Pass） | 57 |
| 不通过（Fail） | 4 |
| 执行率 | 100% |
| 通过率 | 93.4% |

> 不通过用例中，2 条为真实问题（含复现截图证据），2 条经复盘为"用例预期与产品实现/需求口径不符"，不计为产品缺陷（详见第三节）。

## 二、模块执行明细

| 模块 | 用例数 | 通过 | 不通过 |
|---|---|---|---|
| 登录模块 |19|17|2|
| 用户管理 |10|9|1|
| 角色管理 |10|10|0|
| 菜单管理 |5|5|0|
| 部门管理 |5|5|0|
| 系统监控 |3|3|0|
| 定时任务 |4|4|0|
| 接口专项 |5|4|1|
| 兼容性 |10|10|0|
| **合计** | **61** | **57** | **4** |



## 三、不通过用例详情

### 1. TC_API_005｜用户列表接口 pageSize 参数缺少边界校验 —— 真实问题（一般）

| 项 | 内容 |
|---|---|
| 测试标题 | 异常参数传入时接口应做参数校验 |
| 预期结果 | 对负数/超大 pageSize 返回参数错误提示，或限制最大分页大小 |
| 实际结果 | `GET /dev-api/system/user/list?pageNum=1&pageSize=-1` 与 `?pageSize=999999` 均返回 **200**，接口不拒绝、不提示参数错误，直接返回数据（999999 时返回全量 10 条） |
| 严重程度 | 一般（接口健壮性不足，存在接口滥用/全量数据拉取风险） |
| 证据 | evidence/ Postman 复现截图（200 + 响应体） |

### 2. TC_LOGIN_012｜同一账号多浏览器同时登录，无会话互斥 —— 真实问题（一般）

| 项 | 内容 |
|---|---|
| 测试标题 | 同一账号双浏览器同时登录应互斥 |
| 预期结果 | 后登录强制下线旧会话（旧 Token 失效） |
| 实际结果 | 普通窗口 + 无痕窗口均登录成功并正常访问，旧会话未被强制下线 |
| 严重程度 | 一般（安全策略问题；是否必须单点登录取决于产品需求，记录为"待产品确认"） |
| 证据 | evidence/ 双窗口同屏截图 |

### 3. TC_CAPTCHA_00X｜数学验证码负数算式 —— 用例复盘（非缺陷）

| 项 | 内容 |
|---|---|
| 测试标题 | 验证数学验证码算式计算结果（负数场景） |
| 预期结果 | 验证码算式可能出现负数结果并能正确计算 |
| 实际结果 | 实测该框架验证码**从不生成负数结果的算式**，本用例预期与产品实现不符 |
| 复盘结论 | **用例设计假设有误**，非产品缺陷；已调整用例预期/标记无效用例 |

### 4. TC_USER_00X｜新增用户不分配角色保存 —— 需求口径待确认（非缺陷）

| 项 | 内容 |
|---|---|
| 测试标题 | 新增用户不分配角色时保存 |
| 预期结果 | （用例假设）角色为必填项，不分配应保存失败 |
| 实际结果 | 不分配角色也能保存成功（角色为可选字段） |
| 复盘结论 | **需求口径问题**：角色是否必填取决于产品设计，记录为"待产品确认"，不强行定性为缺陷 |

## 四、本次执行结论

- 61 条用例全部执行完毕，核心功能稳定，**未发现严重缺陷**（被测为成熟开源框架，符合预期）
- 发现并记录 **2 个真实问题**：接口参数校验缺失（建议修复）、会话策略问题（建议与产品对齐确认）
- 2 条用例经复盘归类为用例预期/需求口径问题，不计为缺陷
- 全部不通过用例均保留复现步骤与截图证据，可随时复现

## 五、后续建议

1. 接口层增加参数边界校验（如 pageSize 上限）
2. 全局异常统一处理，不向前端返回堆栈信息
3. 会话策略与产品对齐（单点登录或明确多端在线策略）
4. 用例设计前先确认需求口径，避免预期假设与产品实现不符
