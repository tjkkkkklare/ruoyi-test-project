> 文档编号：REQ-RYVUE-001
> 
> 编制目的：本项目为开源实训项目，无官方产品需求文档。本文档参考 RuoYi-Vue 官方使用文档、Web 后台管理系统通用 RBAC 业务规范、页面 UI 交互规则逆向整理，作为后续测试点、测试用例设计的唯一依据。


## 1 项目概述

### 1.1 项目简介

RuoYi-Vue 是基于 SpringBoot + Vue3 开发的前后端分离权限管理系统，采用 RBAC 权限模型，主要用于企业内部人员、角色、菜单、部门统一管控，附带在线用户监控、定时任务管理等运维功能。
### 1.2 测试环境信息

- 前端访问地址：[http://localhost:82](https://link.wtturl.cn/?target=http%3A%2F%2Flocalhost%3A82&scene=im&aid=582478&lang=zh "autolink")
- 后端 API 地址：[http://localhost:8080](https://link.wtturl.cn/?target=http%3A%2F%2Flocalhost%3A8080&scene=im&aid=582478&lang=zh "autolink")
- 数据库：MySQL 8.0，库名 ry-vue
- 认证方式：JWT 令牌鉴权

### 1.3 术语定义

1. RBAC：基于角色的访问控制；用户绑定角色，角色绑定权限
2. 逻辑删除：数据不物理清除，通过字段`del_flag`标记（0 正常，1 删除）
3. 账号禁用：锁定账号，禁止新登录；已登录会话应当失效
4. Token：登录下发的身份令牌，接口请求必须携带合法 Token

## 2 功能需求

### 2.1 登录模块

1. 用户输入用户名、密码完成登录，校验成功下发 JWT Token，跳转首页。
2. 用户名、密码不能为空，参数为空时前端弹出提示，后端拒绝登录。
3. 账号状态为【禁用】时，无法登录系统。
4. 登录成功后，浏览器缓存用户身份信息；点击退出登录，销毁本地 Token，跳转登录页。
5. Token 具备有效期，超时后访问页面自动跳转登录页面。

### 2.2 用户管理模块（sys_user）

1. 新增用户
    
    - 必填项：用户名、密码、所属部门；用户名全局唯一，重复提交系统拦截。
    - 手机号支持格式校验；邮箱支持格式校验。
    - 用户必须分配至少一个角色，否则无法保存。
    
2. 用户状态控制
    
    - 状态分为【正常 / 禁用】；账号禁用后，禁止新登录。
    - 业务约束：账号被禁用后，**当前已登录的会话强制下线**。
    
3. 删除用户
    
    - 执行逻辑删除，修改 del_flag=1；数据库数据保留。
    - 已删除用户默认不在用户列表展示。
    
4. 查询与分页
    
    - 支持按用户名、部门、状态条件筛选查询。
    - 分页参数 pageNum、pageSize 必须为正整数；非法参数后端进行校验，返回友好提示，禁止直接抛出 500 异常堆栈。
    
5. 编辑用户：支持修改昵称、手机号、邮箱、角色、账号状态；用户名不允许修改。

### 2.3 角色管理模块（sys_role）

1. 角色名称、角色标识唯一，不可重复。
2. 支持给角色分配菜单权限；权限保存立即生效。
3. 业务约束：修改角色权限后，**已经登录的用户权限实时生效**。
4. 支持角色启用 / 禁用；禁用角色下的用户无法访问系统功能。
5. 提供数据权限配置（全部数据、本部门数据、自定义数据权限）。

### 2.4 菜单管理模块（sys_menu）

1. 支持新增目录、菜单、按钮权限。
2. 菜单绑定路由地址，仅分配对应权限的用户可见菜单、可访问接口。
3. 未分配菜单权限的用户，无法访问对应页面与接口，返回无权限提示。

### 2.5 部门管理模块（sys_dept）

1. 支持树形层级结构，支持新增、编辑、停用、删除部门。
2. 删除部门前，若存在下属用户 / 子部门，禁止直接删除。

### 2.6 系统监控模块

1. 在线用户监控：展示当前登录账号、登录 IP、登录时间，支持强制下线。
2. 服务监控：展示服务器 CPU、内存、JVM 运行状态。

### 2.7 定时任务模块

1. 支持新增、编辑、启用、暂停定时任务。
2. Cron 表达式格式非法时，前端拦截提示，禁止保存。

## 3 安全相关需求

1. 表单输入框需要过滤 XSS 脚本字符，防止跨站脚本攻击。
2. 所有业务接口必须携带合法 Token；无 Token、Token 伪造、Token 过期，统一返回 401 未授权。
3. 权限隔离：低权限账号不能访问管理员专属接口，拒绝越权访问，返回 403 禁止访问。
4. 后端捕获异常后，禁止向前端输出 Java 堆栈报错信息，统一返回业务化错误提示。

## 4 非功能需求

1. 浏览器兼容性：支持 Chrome、Edge、Firefox 主流浏览器。
2. 数据一致性：前端执行增删改操作后，数据库对应表字段同步更新。
3. 响应规范：接口统一返回 JSON 格式，包含 msg、code、data 字段。

## 5 附录：需求风险说明

1. 本文档为逆向整理模拟需求，若页面实际行为与本文描述冲突，以通用后台系统业务逻辑作为判定依据。
2. 测试过程中发现需求歧义，记录疑问点，作为待确认项。

